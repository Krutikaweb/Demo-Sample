  - name: Upload artifact for GitHub Pages
    uses: actions/upload-pages-artifact@v1
    with:
      path: ./   # repo root (static files)

  - name: Deploy to GitHub Pages
    uses: actions/deploy-pages@v1
